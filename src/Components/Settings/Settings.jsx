import classes from './Settings.module.css'

const Settings = () => {
    return  (
        <section className={classes.settings}>
            Settings
        </section>
    )
}

export default Settings;